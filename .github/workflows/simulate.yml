name: auto-engine-alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: System Check
        run: echo "⚙️ go-auto-engine is running"
